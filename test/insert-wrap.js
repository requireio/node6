var insertWrapper = require('../lib/insert-wrap')

var test = require('tape')

test('', function(t) {

node -v
node -e "console.log('ok')"
node -p "2 * 3"
node --interactive
node --no-deprecation file
node file -a -b -c arg1
node file arg1 -a -b -c arg2
node debug file -a -b -c arg1
node debug file arg1 -a -b -c arg2
node debug
})
insertWrapper()
